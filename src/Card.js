import React, {useState} from 'react';import DeleteModal from "./DeleteModal";import UpdateModal from "./UpdateModal";const Card = (props) => {    const [open, setOpen] = useState(false);    const [updateOpen, setUpdateOpen] = useState(false)    const toggle = () => {        setOpen(!open)    };    const updateToggle = () => {        setUpdateOpen(!updateOpen)    }    return (        <div className="card">            <div className="card-body">                <h5 className="card-title">{props.card.name}</h5>                <p className="card-text">{props.card.description}</p>                <p>                    <button disabled={props.card.priority < 2}                            onClick={() => props.changePriority(props.card.id, -1)}>⬇️                    </button>                    {' '}                    {props.card.priority}                    {' '}                    <button disabled={props.card.priority > 4}                            onClick={() => props.changePriority(props.card.id, +1)}>⬆️                    </button>                </p>                <>                    <button disabled={props.status === "todo"}                            onClick={() => props.changeStatus(props.card.id, -1)}>⬅️                    </button>                    <button onClick={updateToggle}>✏️</button>                    {updateOpen &&                        <UpdateModal                            open={open}                            card={props.card}                            updateTask={props.updateTask}                            header={"Update"}                            statuses={props.statuses}                            priorities={props.priorities}                            updateToggle={updateToggle}                            updateOpen={updateOpen}/>}                    <DeleteModal                        open={open}                        deleteTask={props.deleteTask}                        card={props.card}/>                    <button disabled={props.status === "done"}                            onClick={() => props.changeStatus(props.card.id, +1)}>➡️                    </button>                </>            </div>        </div>    );};export default Card;