import {v4 as uuidv4} from 'uuid';import React, {useState} from 'react';import {Button, Modal, ModalHeader, ModalBody, ModalFooter, InputGroupText, InputGroup} from 'reactstrap';function UpdateModal(props) {    // const toggle = () => setOpen(!open)    const {updateToggle, updateOpen}=props// const [newCard, setNewCard]=useState(props.card)    const [name, setName] = useState(props.card.name)    const [description, setDescription] = useState(props.card.description)    const [status, setStatus] = useState(props.card.status.toUpperCase())    const [priority, setPriority] = useState(props.card.priority)    const updateButtonHandler=()=>{        const newTask={            id:props.card.id,            name,            description,            priority,            status:status.toLowerCase()}        props.updateTask(props.card.id, newTask)        updateToggle()        console.log(newTask)    }    return (        <>            <Modal isOpen={updateOpen} toggle={updateToggle}>                <ModalHeader toggle={updateToggle}>Update</ModalHeader>                <ModalBody>                    <InputGroup>                        <InputGroupText>                            {props.card.name}                            <input value={name} onChange={e => setName(e.target.value)}/>                        </InputGroupText>                    </InputGroup>                    <br/>                    <InputGroup>                        <InputGroupText>                            {props.card.description}                            <input value={description} onChange={e => setDescription(e.target.value)}/>                        </InputGroupText>                    </InputGroup>                    <br/>                    <InputGroup>                        <InputGroupText>                            Priority                        </InputGroupText>                        <select className="form-select" aria-label="Default select example" value={priority}                                onChange={(e) => setPriority(e.target.value)}>                            {props.priorities.map((el, i) =>                                <option value={el} key={i}>{el}</option>                            )}                        </select>                    </InputGroup>                    <br/>                    <InputGroup>                        <InputGroupText>                            Status                        </InputGroupText>                        <select className="form-select" aria-label="Default select example" value={status}                                onChange={(e) => setStatus(e.target.value)}>                            {props.statuses.map((el, i) =>                                <option value={el} key={i}>{el.toUpperCase()} </option>                            )}                        </select>                    </InputGroup>                </ModalBody>                <ModalFooter>                    <Button color="danger" onClick={updateButtonHandler}>                        Yes                    </Button>{' '}                    <Button color="secondary" onClick={updateToggle}>                        No                    </Button>                </ModalFooter>            </Modal>        </>    );}export default UpdateModal;